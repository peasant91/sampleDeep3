import React from 'react'
import { SafeAreaView, View } from 'react-native'
import NavBar from '../components/atoms/NavBar'


const StartContractScreen = ({navigation, route}) => {
    return <SafeAreaView>
        <NavBar />
        <View>
            <View>

            </View>
            <View style={{padding}}/>
        </View>
    </SafeAreaView>
}

export default StartContractScreen